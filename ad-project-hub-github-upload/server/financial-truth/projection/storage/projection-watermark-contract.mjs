const digest=/^[0-9a-f]{64}$/;
export function validateProjectionWatermark(w){if(!w||!Number.isSafeInteger(w.eventCount)||w.eventCount<0||typeof w.canonicalDigest!=="string"||!digest.test(w.canonicalDigest))throw new Error("PROJECTION_ROW_INVALID");if(w.eventCount===0&&w.latestCanonicalEventId!==null)throw new Error("PROJECTION_ROW_INVALID");if(w.eventCount>0&&(typeof w.latestCanonicalEventId!=="string"||!w.latestCanonicalEventId))throw new Error("PROJECTION_ROW_INVALID");return true;}
export function sameProjectionWatermark(a,b){return a===null&&b===null||!!a&&!!b&&a.eventCount===b.eventCount&&a.latestCanonicalEventId===b.latestCanonicalEventId&&a.canonicalDigest===b.canonicalDigest;}
export function sameMaterializedProjectionState(a,b){if(!a||!b)return false;const keys=["scopeType","companyId","projectId","currency","status","cashMinor","paidMinor","receivableMinor","costMinor","payableMinor","recognizedRevenueMinor","projectionContractVersion","rebuiltAt"];return keys.every(k=>a[k]===b[k])&&sameProjectionWatermark(a.watermark,b.watermark);}
