import { validateRebuildRequest } from "./rebuild-input-bundle.mjs";
import { buildFinancialRelationshipGraph } from "../../history.mjs";
import { createFinancialEvent } from "../../financial-event.mjs";
export function validateRelationshipCompleteBundle(bundle,request){validateRebuildRequest(request);if(!bundle||bundle.relationshipCompletenessStatus!=="COMPLETE"||bundle.scopeType!==request.scopeType||bundle.companyId!==request.companyId||bundle.currency!==request.currency||(request.scopeType==="PROJECT"&&bundle.projectId!==request.projectId)||(request.scopeType==="COMPANY"&&bundle.projectId!=null)||!Array.isArray(bundle.rawEvents))throw new Error("REBUILD_INPUT_INVALID");try{const events=bundle.rawEvents.map(createFinancialEvent);buildFinancialRelationshipGraph(events);for(const e of events){if(e.companyId!==request.companyId||e.currency!==request.currency||(request.scopeType==="PROJECT"&&e.projectId!==request.projectId))throw new Error("REBUILD_INPUT_INVALID");}return Object.freeze({...bundle,rawEvents:Object.freeze(events)});}catch(e){throw new Error("REBUILD_INPUT_INVALID");}}
