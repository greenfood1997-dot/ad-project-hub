import assert from "node:assert/strict"; import { RebuildOrchestrator } from "../server/financial-truth/projection/orchestration/index.mjs";
const p={watermark:{eventCount:1}};const bundle={scopeType:"COMPANY",companyId:"c",projectId:null,currency:"CNY",rawEvents:[],relationshipCompletenessStatus:"COMPLETE"};const calls=[];const repo={async getCompanyProjection(){calls.push("read");return null;},async saveCompanyProjection(_,x,e){calls.push(["save",e]);return "SAVED";}};const o=new RebuildOrchestrator({inputProvider:{async getCompanyRebuildInput(){calls.push("provider");return bundle;}},projectionRepository:repo,p1Rebuild(){calls.push("p1");return p;}});assert.equal((await o.rebuildCompanyProjection({context:{},companyId:"c",currency:"CNY"})).status,"REBUILD_SAVED");assert.deepEqual(calls.map(x=>Array.isArray(x)?x[0]:x),["read","provider","p1","save"]);console.log("Financial Truth projection rebuild orchestration regression: PASS");
