import assert from "node:assert/strict"; import * as s from "../server/financial-truth/projection/storage/index.mjs";
const wm={eventCount:1,latestCanonicalEventId:"e1",canonicalDigest:"a".repeat(64)}; const company={projectionId:"p",scopeType:"COMPANY",companyId:"c",projectId:null,currency:"CNY",status:"CURRENT",cashMinor:0,paidMinor:null,receivableMinor:1,costMinor:null,payableMinor:0,recognizedRevenueMinor:0,watermark:wm,projectionContractVersion:1,rebuiltAt:"2026-01-01T00:00:00.000Z",updatedAt:null}; const project={...company,scopeType:"PROJECT",projectId:"p1",cashMinor:null,paidMinor:0,costMinor:0};
for(const p of [company,project]){const row=s.projectionToPostgresRow(p); const back=s.postgresRowToProjection(row); assert.equal(back.companyId,p.companyId); assert.equal(back.currency,p.currency)}
for(const bad of [{...company,scopeType:"X"},{...company,projectId:"p"},{...company,cashMinor:null},{...project,projectId:null},{...company,status:"INVALID_HISTORY"},{...company,watermark:{...wm,eventCount:0,latestCanonicalEventId:"e"}}]) assert.throws(()=>s.projectionToPostgresRow(bad));
assert.equal(s.sameProjectionWatermark(wm,{...wm}),true); assert.equal(s.sameMaterializedProjectionState(company,{...company,projectionId:"other",updatedAt:"x"}),true); assert.equal(s.PROJECTION_ROW_FIELDS.length,18); console.log("Financial Truth projection storage contract regression: PASS");
